# Frontend

React + Vite. Пустой каркас со страницей логина.

```
src/
  api.ts
  auth.tsx
  types.ts
  App.tsx
  main.tsx
  pages/LoginPage.tsx
```

```bash
cd frontend && npm install && npm run dev
```

http://localhost:5173 — логин по токену, после входа пустая страница `/`.
